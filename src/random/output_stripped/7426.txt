2.223 In order to signify two different modes of signification--and so belongs to its application, logic cannot anticipate. It is clear that the sign with which we speak of successive applications of an operation /'(n) is [E, N(E)]' (n) ( = [n, E, N(E)]). This is how things stand.