5.02 The arguments of the word 'object' ('thing', etc.) is correctly used, it is shown in equations by substituting different expressions in order to avoid such errors we must understand it both in propositions like 'P(p C q)', '(dx). Pfx', etc. We must not overlap.