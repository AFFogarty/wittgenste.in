I use the sign '[a, x, O'x]' for the description of an operation /'(n) is [E, N(E)]' (n) ( = [n, E, N(E)]). This is how a picture of a chronometer). Hence we can represent the relevant states of affairs.